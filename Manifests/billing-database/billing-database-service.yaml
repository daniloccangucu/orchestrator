apiVersion: v1
kind: Service
metadata:
  name: billing-database
  labels:
    app: billing-database
spec:
  ports:
  - port: 5432
    name: postgres
  clusterIP: None
  selector:
    app: billing-database
